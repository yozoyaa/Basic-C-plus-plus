// Strings are used to store sequences of characters and are supported by the standard library std::string.

#include <iostream>
#include <string>

int main() {
    std::string myString = "Hello, Strings!";
    std::cout << myString << std::endl;
    return 0;
}
